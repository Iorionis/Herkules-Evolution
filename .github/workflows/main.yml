name: Herkules Daily Evolution

on:
  schedule:
    - cron: '0 */6 * * *'  # Co 6 godzin
  workflow_dispatch:

jobs:
  herkules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install flask requests
      
      - name: Run Herkules
        run: |
          echo "✅ Herkules Daily Pulse Active!"
          python -c "print('Herkules is ALIVE!')"